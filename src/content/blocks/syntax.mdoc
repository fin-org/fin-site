## Intuitive Syntax

Data interchange between programs doesn’t require comments, commas, newlines or indentation to
correctly convey values. However, fin is designed for us too, so it does have syntax elements for
humans.

In fin these elements directly control the output in an obvious, canonical way. The idea (inspired
by `zig fmt`) is showcased in the example. Formatting is built-in with zero configuration.
